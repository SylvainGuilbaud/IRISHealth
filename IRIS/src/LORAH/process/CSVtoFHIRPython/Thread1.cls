/// This class is generated by the LORAH.process.CSVtoFHIRPython BPL class.
/// This class should not be edited or modified in any way.
/// Do not make direct use of this class in your application.
Class LORAH.process.CSVtoFHIRPython.Thread1 Extends Ens.BP.Thread [ ClassType = persistent, CompileAfter = LORAH.process.CSVtoFHIRPython, GeneratedBy = Ens.BPL.Compiler.CLS, ProcedureBlock ]
{

/// Generated. DO NOT EDIT. BPLShape:Code: for LORAH.process.CSVtoFHIRPython.Thread1
ClassMethod pyExec1(process As Ens.BusinessProcess, context As Ens.BP.Context, request As %Library.Persistent, response As %Library.Persistent, status As Ens.Util.PyByRef) As %Library.String [ Language = python ]
{
import iris
import traceback
status.value=iris.cls('%SYSTEM.Status').OK()
pErrorMessage=""
try:
 import csv
 import requests
 import json
 from datetime import datetime
 from zoneinfo import ZoneInfo
 paris_tz = ZoneInfo("Europe/Paris")
 id = request.id
 patient = {
                                 "resourceType": "Patient",
                                 "id": request.id,
                                 "identifier": [
                                         {
                                                 "system": "http://hospital.smarthealthit.org",
                                                 "value": request.id
                                         },
                                         {
                                                 "type": {
                                                         "coding": [
                                                                 {
                                                                         "system": "http://terminology.hl7.org/CodeSystem/v2-0203",
                                                                         "code": "MR",
                                                                         "display": "Medical Record Number"
                                                                 }
                                                         ],
                                                         "text": "Medical Record Number"
                                                 },
                                                 "system": "http://hospital.smarthealthit.org",
                                                 "value": request.id
                                         },
                                         {
                                                 "type": {
                                                         "coding": [
                                                                 {
                                                                         "system": "http://terminology.hl7.org/CodeSystem/v2-0203",
                                                                         "code": "SS",
                                                                         "display": "Social Security Number"
                                                                 }
                                                         ],
                                                         "text": "Social Security Number"
                                                 },
                                                 "system": "http://hl7.org/fhir/sid/us-ssn",
                                                 "value": request.id
                                         }
                                 ],
                                 "name": [
                                 {
                                         "use": "official",
                                         "family": request.nom,
                                         "given": [
                                                 request.prenom
                                         ]
                                 }
                                 ],
                                 "gender": request.sexe,
                                 "birthDate": request.datedenaissance
                         }
 headers = {
                           'Content-Type': 'application/fhir+json'
                         }
 result = requests.put(f"http://host.docker.internal:18880/lorah/fhir/r4/Patient/{id}",json=patient,headers=headers)
 now = datetime.now(paris_tz)
 timestamp = now.strftime('%d/%m/%Y %H:%M:%S:%f %Z')
 if result.status_code == 201:
     response.StringValue=timestamp + " Patient " + request.nom + request.prenom + " créé avec succès"
 elif result.status_code == 200:
     response.StringValue=timestamp + " Patient " + request.nom + request.prenom + " mis à jour avec succès "
 else:
     response.StringValue=timestamp + " Erreur lors de la création du patient " + request.nom + request.prenom + "status_code:" + result.status_code + " text:" + result.text
 
 
                         
except:
 pErrorMessage='BPLShape:Code:'+(traceback.format_exc())
 status.value=iris.cls('%SYSTEM.Status').Error(2603,pErrorMessage)
 return ''
}

Method S1(process As Ens.BusinessProcess, context As Ens.BP.Context, synctimedout As %Boolean, syncresponses As %ArrayOfObjects(ELEMENTTYPE="%Library.Persistent"), request As %Library.Persistent, response As %Library.Persistent) As %Status [ Language = objectscript, PublicList = (process, context) ]
{
 Set $ZT="Trap",status=$$$OK do {
   Set statusensPyExecReturnVariable=##class(Ens.Util.PyByRef).%New()
   Do ..pyExec1(.process, .context, .request, .response, .statusensPyExecReturnVariable)
   Set status=statusensPyExecReturnVariable.value
   If $$$ISERR(status) Quit
 Do ##class(Ens.Util.Trace).WriteTrace("user","LORAH.process.CSVtoFHIRPython","S1",response.StringValue)
 Do process.ClearAllPendingResponses()
 Set ..%NextState="Stop"
 } while (0)
Exit Quit ..ManageState(status)
Trap Set $ZT="",status=..ManageStatus(status,"S1") Goto Exit
}

Storage Default
{
<Type>%Storage.Persistent</Type>
}

}
