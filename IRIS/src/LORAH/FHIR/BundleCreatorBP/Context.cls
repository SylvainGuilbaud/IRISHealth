/// This class is generated by the LORAH.FHIR.BundleCreatorBP BPL class.
/// This class should not be edited or modified in any way.
/// Do not make direct use of this class in your application.
Class LORAH.FHIR.BundleCreatorBP.Context Extends Ens.BP.Context [ ClassType = persistent, CompileAfter = LORAH.FHIR.BundleCreatorBP, GeneratedBy = Ens.BPL.Compiler.CLS, ProcedureBlock ]
{

/// Response Handler for call to FhirBundleCreatorBO for pCompiler.CurrentMethod LORAH.FHIR.BundleCreatorBP.Context
Method OnResponse1(process As Ens.BusinessProcess, context As Ens.BP.Context, request As Ens.StreamContainer, response As Ens.StringResponse, callrequest As Ens.StreamContainer, callresponse As Ens.StreamContainer, callname As %String) As %Status [ Language = objectscript, PublicList = (process, context) ]
{
 Set $ZT="Trap",status=$$$OK do {
 Set status=$$$OK,request.Stream=callresponse.Stream
 If $$$ISERR(status) Quit
 Set status=$$$OK,request.OriginalFilename=callresponse.OriginalFilename
 If $$$ISERR(status) Quit
 } while (0)
Exit Quit status
Trap Set $ZT="",status=##class(Ens.Util.Error).EnsSystemError("LORAH.FHIR.BundleCreatorBP.Thread1","OnResponse1")
 Goto Exit
}

/// Response Handler for call to sendIRISHealth for pCompiler.CurrentMethod LORAH.FHIR.BundleCreatorBP.Context
Method OnResponse2(process As Ens.BusinessProcess, context As Ens.BP.Context, request As Ens.StreamContainer, response As Ens.StringResponse, callrequest As EnsLib.REST.GenericMessage, callresponse As EnsLib.REST.GenericMessage, callname As %String) As %Status [ Language = objectscript, PublicList = (process, context) ]
{
 Set $ZT="Trap",status=$$$OK do {
 Set status=$$$OK,response.StringValue=callresponse.HTTPHeaders.GetAt("StatusLine")
 If $$$ISERR(status) Quit
 } while (0)
Exit Quit status
Trap Set $ZT="",status=##class(Ens.Util.Error).EnsSystemError("LORAH.FHIR.BundleCreatorBP.Thread1","OnResponse2")
 Goto Exit
}

/// Response Handler for call to SendFHIRBundleToServerBO for pCompiler.CurrentMethod LORAH.FHIR.BundleCreatorBP.Context
Method OnResponse3(process As Ens.BusinessProcess, context As Ens.BP.Context, request As Ens.StreamContainer, response As Ens.StringResponse, callrequest As EnsLib.REST.GenericMessage, callresponse As EnsLib.REST.GenericMessage, callname As %String) As %Status [ Language = objectscript, PublicList = (process, context) ]
{
 Set $ZT="Trap",status=$$$OK do {
 Set status=$$$OK,response.StringValue=callresponse.HTTPHeaders.GetAt("StatusLine")
 If $$$ISERR(status) Quit
 } while (0)
Exit Quit status
Trap Set $ZT="",status=##class(Ens.Util.Error).EnsSystemError("LORAH.FHIR.BundleCreatorBP.Thread1","OnResponse3")
 Goto Exit
}

Storage Default
{
<Type>%Storage.Persistent</Type>
}

}
