/// This class is generated by the SC.BP.MfgSalesOrderWorkFlowProcess BPL class.
/// This class should not be edited or modified in any way.
/// Do not make direct use of this class in your application.
Class SC.BP.MfgSalesOrderWorkFlowProcess.Thread1 Extends Ens.BP.Thread [ ClassType = persistent, CompileAfter = SC.BP.MfgSalesOrderWorkFlowProcess, GeneratedBy = Ens.BPL.Compiler.CLS, ProcedureBlock ]
{

Method S1(process As Ens.BusinessProcess, context As Ens.BP.Context, synctimedout As %Boolean, syncresponses As %ArrayOfObjects(ELEMENTTYPE="%Library.Persistent"), request As %Library.Persistent, response As %Library.Persistent) As %Status [ Language = objectscript, PublicList = (process, context) ]
{
 Set $ZT="Trap",status=$$$OK do {
 Set status=$$$OK,context.issueId=request.issueId
 If $$$ISERR(status) Quit
 Do {
 set context.salesorder =context.getImpactedObject()
 } while (0)
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.analysis.processName")
 Set status=$$$OK,context.analysis.processName=request.targetProcess
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.analysis.severity")
 Set status=$$$OK,context.analysis.severity=2
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.analysis.urgency")
 Set status=$$$OK,context.analysis.urgency=3
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.analysis.impactAnalysis")
 Set status=$$$OK,context.analysis.impactAnalysis="Serious customer satisfaction concerns, and a penalty of $50K."
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.analysis.rootCauseAnalysis")
 Set status=$$$OK,context.analysis.rootCauseAnalysis="Order expected to be late due to a delayed maintenance caused by a late shipment of a part needed. "
 If $$$ISERR(status) Quit
 Do {
 do context.createNewScenario()
 } while (0)
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.newScenario.optionName")
 Set status=$$$OK,context.newScenario.optionName="Option A"
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.newScenario.optionNumber")
 Set status=$$$OK,context.newScenario.optionNumber="A"
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.newScenario.description")
 Set status=$$$OK,context.newScenario.description="Move the sales order to Hamburg plant"
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.newScenario.supportingData")
 Set status=$$$OK,context.newScenario.supportingData="Impact: The order will be fulfilled on time, with extra $1500 shipping cost. Extra work load on Hamburg plant will require an early maintenance window."
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.newScenario.costImpact")
 Set status=$$$OK,context.newScenario.costImpact=1500
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.newScenario.timeImpact")
 Set status=$$$OK,context.newScenario.timeImpact=36
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.newScenario.feasibility")
 Set status=$$$OK,context.newScenario.feasibility=1
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.newScenario.recommended")
 Set status=$$$OK,context.newScenario.recommended=1
 If $$$ISERR(status) Quit
 Do {
 do context.addScenarioToAnalysis()
 } while (0)
 If $$$ISERR(status) Quit
 Do {
 do context.createNewScenario()
 } while (0)
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.newScenario.optionName")
 Set status=$$$OK,context.newScenario.optionName="Option B"
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.newScenario.optionNumber")
 Set status=$$$OK,context.newScenario.optionNumber="A"
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.newScenario.description")
 Set status=$$$OK,context.newScenario.description="Wait for the part, complete the maintenance, and use expedited shipping once done."
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.newScenario.supportingData")
 Set status=$$$OK,context.newScenario.supportingData="Impact: $2000 extra shipping cost. Order expected to be 2 days late."
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.newScenario.costImpact")
 Set status=$$$OK,context.newScenario.costImpact=500
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.newScenario.timeImpact")
 Set status=$$$OK,context.newScenario.timeImpact=0
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.newScenario.feasibility")
 Set status=$$$OK,context.newScenario.feasibility=1
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.newScenario.recommended")
 Set status=$$$OK,context.newScenario.recommended=0
 If $$$ISERR(status) Quit
 Do {
 do context.addScenarioToAnalysis()
 } while (0)
 If $$$ISERR(status) Quit
 Do {
 do context.createNewScenario()
 } while (0)
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.newScenario.optionName")
 Set status=$$$OK,context.newScenario.optionName="Option C"
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.newScenario.optionNumber")
 Set status=$$$OK,context.newScenario.optionNumber="C"
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.newScenario.description")
 Set status=$$$OK,context.newScenario.description="Leverage spare parts in Munich plant and redirect current shipment to Munich."
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.newScenario.supportingData")
 Set status=$$$OK,context.newScenario.supportingData="Impact: Current sales order will be done on time, but this will put $250,000 revenue at risk at Munich plant due to the delayed maintenance."
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.newScenario.costImpact")
 Set status=$$$OK,context.newScenario.costImpact=12000
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.newScenario.timeImpact")
 Set status=$$$OK,context.newScenario.timeImpact=0
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.newScenario.feasibility")
 Set status=$$$OK,context.newScenario.feasibility=1
 If $$$ISERR(status) Quit
 Do ##class(Ens.DataTransform).instantiateProps(context,"context.newScenario.recommended")
 Set status=$$$OK,context.newScenario.recommended=0
 If $$$ISERR(status) Quit
 Do {
 do context.addScenarioToAnalysis()
 } while (0)
 If $$$ISERR(status) Quit
 #; --- Begin Call Create workflow ---
 #; --- Begin Request ---
 Set callrequest=##class(EnsLib.Workflow.TaskRequest).%New()
 If '$IsObject(callrequest) Set status=%objlasterror Quit
 Set status=$$$OK,callrequest.%Actions="A,B"
 If $$$ISERR(status) Quit
 Set status=$$$OK,callrequest.%Message="A. Use expedite shipping; B. Offer free shipping"
 If $$$ISERR(status) Quit
 Set status=$$$OK,callrequest.%Subject="User input required for issue "_context.analysis.issueId
 If $$$ISERR(status) Quit
 Set status=..MakeAsyncCall("OnResponse1","Workflow Operation",callrequest,"Create workflow",1,0,"Create workflow",0)
 If $$$ISERR(status) Quit
 Kill callrequest
 #; --- End Request ---
 #; --- End Call Create workflow ---
 Do {
 set context.analysis.workflowId = $ListGet(process.%MasterPendingResponses.GetAt(1),1)
 do context.saveWorkflowAnalysisToIssue()
 } while (0)
 If $$$ISERR(status) Quit
 Do ..%PendingResponses.Clear(),..%SyncResponses.Clear()
 Set ..%SyncTimedOut=0,..%PendingTimeout="",..%PendingAlarmHandle="",..%SyncName=""
 If ..%Process.IsResponsePending("Create workflow") Do ..%PendingResponses.SetAt("","Create workflow")
 If ..%PendingResponses.Count()=0 {
    Set ..%NextState="S3"
 } else {
    Set ..%NextState="S2",..%Wait=1
 }
 } while (0)
Exit Quit ..ManageState(status)
Trap Set $ZT="",status=..ManageStatus(status,"S1") Goto Exit
}

Method S2(process As Ens.BusinessProcess, context As Ens.BP.Context, synctimedout As %Boolean, syncresponses As %ArrayOfObjects(ELEMENTTYPE="%Library.Persistent"), request As %Library.Persistent, response As %Library.Persistent) As %Status [ Language = objectscript, PublicList = (process, context) ]
{
 Set $ZT="Trap",status=$$$OK do {
 If ..%PendingResponses.Count()=0 {
    Set ..%NextState="S3"
 } else {
    Set ..%NextState="S2",..%Wait=1
 }
 } while (0)
Exit Quit ..ManageState(status)
Trap Set $ZT="",status=..ManageStatus(status,"S2") Goto Exit
}

Method S3(process As Ens.BusinessProcess, context As Ens.BP.Context, synctimedout As %Boolean, syncresponses As %ArrayOfObjects(ELEMENTTYPE="%Library.Persistent"), request As %Library.Persistent, response As %Library.Persistent) As %Status [ Language = objectscript, PublicList = (process, context) ]
{
 Set $ZT="Trap",status=$$$OK do {
 Do ##class(Ens.Util.Trace).WriteTrace("user","SC.BP.MfgSalesOrderWorkFlowProcess","S3",context.taskId)
 Do process.ClearAllPendingResponses()
 Set ..%NextState="Stop"
 } while (0)
Exit Quit ..ManageState(status)
Trap Set $ZT="",status=..ManageStatus(status,"S3") Goto Exit
}

Storage Default
{
<Type>%Storage.Persistent</Type>
}

}
